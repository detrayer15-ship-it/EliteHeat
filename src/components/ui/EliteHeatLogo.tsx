export const EliteHeatLogo = ({ className = "w-10 h-10" }: { className?: string }) => {
    return (
        <img
            src="/images/logo.png"
            alt="EliteHeat Logo"
            className={`${className} object-contain`}
        />
    )
}
