# 🔧 ДИАГНОСТИКА И ИСПРАВЛЕНИЕ

## Проблемы:

1. ❌ AI не предлагает варианты стилей
2. ❌ Проект не открывается после создания
3. ❌ Gemini не работает

## Решения:

### 1. Тест Gemini API

Создан тестовый компонент: `src/components/test/GeminiTest.tsx`

**Как использовать:**

Добавьте в `Dashboard.tsx`:

```typescript
import { GeminiTest } from '@/components/test/GeminiTest'

// В JSX
<GeminiTest />
```

Откройте Dashboard и протестируйте Gemini!

### 2. Проверка ошибок в консоли

Откройте браузер (F12) → Console

**Ищите:**
- `Gemini API Error:` - ошибки API
- `Error fetching project:` - ошибки загрузки проекта
- `Error creating project:` - ошибки создания

### 3. Проверка Firebase

**Откройте Firebase Console:**
1. Перейдите на https://console.firebase.google.com
2. Выберите проект `eliteheat-da70c`
3. Firestore Database → `projects` коллекция
4. Проверьте есть ли созданные проекты

### 4. Проверка сохранения проектов

**В коде `ProjectCreationChat.tsx` строка 160:**

```typescript
const docRef = await addDoc(collection(db, 'projects'), projectData)

// Сохраняем также в локальное хранилище
createLocalProject({ ...projectData, id: docRef.id })
```

**Проверьте что обе строки есть!**

### 5. Проверка роутинга

**В `App.tsx` должен быть роут:**

```typescript
<Route
  path="/projects/:id"
  element={
    <ProtectedRoute>
      <ProjectDashboardPage />
    </ProtectedRoute>
  }
/>
```

## Пошаговая диагностика:

### Шаг 1: Проверьте Gemini API

```bash
# Откройте консоль браузера
# Перейдите на Dashboard
# Создайте проект
# Смотрите есть ли ошибки
```

**Возможные ошибки:**

- `404 not found` → Модель не найдена (используется fallback)
- `403 forbidden` → API ключ неверный
- `429 too many requests` → Превышен лимит

### Шаг 2: Проверьте создание проекта

```typescript
// В ProjectCreationChat.tsx добавьте логи:

const docRef = await addDoc(collection(db, 'projects'), projectData)
console.log('✅ Проект создан в Firebase:', docRef.id)

createLocalProject({ ...projectData, id: docRef.id })
console.log('✅ Проект сохранён локально')
```

### Шаг 3: Проверьте открытие проекта

```typescript
// В ProjectDashboardPage.tsx добавьте лог:

useEffect(() => {
  console.log('📊 Загрузка проекта:', id)
  console.log('📊 Проект загружен:', project)
  console.log('📊 Ошибка:', error)
}, [id, project, error])
```

## Быстрое исправление:

### Если Gemini не работает:

**Проверьте API ключ в `src/api/gemini.ts`:**

```typescript
const API_KEY = 'AIzaSyCk7v9spUdCGeT9P1Blfopia1_Brc9lb08'
```

**Попробуйте другой ключ из `.env.example`:**

```typescript
const API_KEY = 'AIzaSyCjZ6u_7uG128pM-9Y1u0MNN3ulk6xmMuo'
```

### Если проект не открывается:

**Проверьте что `userId` сохраняется:**

```typescript
// В ProjectCreationChat.tsx строка ~120:

const projectData = {
  userId: user?.uid, // ← ВАЖНО!
  title: analysis.title,
  // ...
}
```

### Если варианты стилей не показываются:

**Интегрируйте в ProjectCreationChat:**

```typescript
import { suggestDesignStyles } from '@/utils/designStyleSuggestions'
import { DesignStyleSuggestions } from '@/components/project/DesignStyleSuggestions'

// После анализа идеи:
const styles = await suggestDesignStyles(userMessage)
if (styles) {
  // Показать варианты
}
```

## Тестирование:

1. **Откройте Dashboard**
2. **Добавьте `<GeminiTest />`**
3. **Протестируйте Gemini**
4. **Создайте проект**
5. **Проверьте консоль на ошибки**
6. **Откройте "Мои проекты"**
7. **Кликните на проект**
8. **Проверьте открывается ли**

## Если ничего не помогает:

Откройте консоль браузера (F12) и скопируйте ВСЕ ошибки красным цветом!

Это поможет найти точную проблему! 🔍
