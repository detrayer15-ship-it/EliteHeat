# 🎨 AI Предлагает Варианты Стилей Дизайна

## Что это?

Когда студент описывает проект, AI автоматически предлагает **3 варианта стиля дизайна** с:
- Названием стиля
- Описанием
- Цветовой палитрой
- Настроением
- Примерами

## Пример работы:

### Студент пишет:
```
"Хочу создать сайт для продажи кофе в минималистичном стиле"
```

### AI предлагает:

**🎨 AI предлагает варианты стиля:**

**1. Скандинавский минимализм**
- Описание: Светлые тона, много воздуха, простые формы
- Палитра: `#FFFFFF` `#F5F5F5` `#8B7355`
- Настроение: Уютный
- Примеры: IKEA, Muji

**2. Японский минимализм**
- Описание: Природные материалы, баланс, гармония
- Палитра: `#FAFAFA` `#2C2C2C` `#D4A574`
- Настроение: Спокойный
- Примеры: Muji, Uniqlo

**3. Современный tech-минимализм**
- Описание: Чёткие линии, контрастные акценты
- Палитра: `#FFFFFF` `#000000` `#FF6B35`
- Настроение: Профессиональный
- Примеры: Apple, Notion

## Как использовать:

### В ProjectCreationChat:

```typescript
import { suggestDesignStyles } from '@/utils/designStyleSuggestions'
import { DesignStyleSuggestions } from '@/components/project/DesignStyleSuggestions'

// В компоненте
const [designSuggestions, setDesignSuggestions] = useState(null)

// После того как студент описал проект
const handleAnalyze = async (idea: string) => {
  // Получаем варианты стилей
  const styles = await suggestDesignStyles(idea)
  setDesignSuggestions(styles)
}

// В JSX
{designSuggestions && (
  <DesignStyleSuggestions
    suggestions={designSuggestions.suggestions}
    onSelect={(style) => {
      console.log('Студент выбрал:', style)
      // Применить стиль к проекту
    }}
  />
)}
```

### Полный пример интеграции:

```typescript
const handleSend = async () => {
  const userMessage = input.trim()
  
  // 1. Студент описывает проект
  setMessages(prev => [...prev, { role: 'user', content: userMessage }])
  
  // 2. AI анализирует и предлагает стили
  const styles = await suggestDesignStyles(userMessage)
  
  // 3. Показываем варианты
  if (styles) {
    setMessages(prev => [...prev, {
      role: 'assistant',
      content: 'Отличная идея! Вот несколько вариантов стиля:',
      designSuggestions: styles.suggestions
    }])
  }
}
```

## Где работает:

- ✅ **Dashboard** → Создание проекта
- ✅ **AI Assistant** → Консультация по дизайну
- ✅ **Project Storyboard** → Выбор стиля презентации

## Технические детали:

### Функция `suggestDesignStyles`:
```typescript
// Вход: описание проекта
const styles = await suggestDesignStyles("сайт для кофе")

// Выход:
{
  detectedStyle: "Минимализм",
  suggestions: [
    {
      name: "Скандинавский минимализм",
      description: "...",
      colors: ["#FFFFFF", "#F5F5F5", "#8B7355"],
      mood: "Уютный",
      examples: "IKEA, Muji"
    },
    // ... ещё 2 варианта
  ]
}
```

### Компонент `DesignStyleSuggestions`:
- Отображает карточки стилей
- Показывает цветовую палитру
- Кликабельные для выбора
- Красивая анимация при наведении

## Примеры использования:

### 1. Студент: "Игра для детей"
AI предлагает:
- Яркий и игривый
- Мультяшный
- Образовательный

### 2. Студент: "Приложение для бизнеса"
AI предлагает:
- Корпоративный
- Минималистичный профессиональный
- Премиум

### 3. Студент: "Сайт для музыки"
AI предлагает:
- Тёмная тема с неоном
- Яркие градиенты
- Ретро-винил

## Преимущества:

✅ AI понимает контекст проекта
✅ Предлагает подходящие стили
✅ Показывает конкретные цвета
✅ Даёт примеры для вдохновения
✅ Студент может выбрать или продолжить со своим

## Готово к использованию!

Файлы созданы:
- `src/utils/designStyleSuggestions.ts` - функция AI
- `src/components/project/DesignStyleSuggestions.tsx` - компонент UI

Интегрируйте в ProjectCreationChat! 🎨
