# 🚀 ПРОСТОЙ СПОСОБ: Установка роли Developer

## ✅ СПОСОБ 1: Через приложение (САМЫЙ ПРОСТОЙ)

### Шаги:

1. **Запустите приложение:**
   ```bash
   npm run dev
   ```

2. **Откройте в браузере:**
   ```
   http://localhost:5174/dev-setup
   ```

3. **Готово!** Вы увидите:
   ```
   ✅ Роль DEVELOPER установлена! Перезагрузите страницу.
   ```

4. **Перезагрузите страницу** (F5)

---

## ✅ СПОСОБ 2: Через Firebase Console

### Шаги:

1. **Откройте Firebase Console:**
   ```
   https://console.firebase.google.com/
   ```

2. **Выберите проект EliteHeat**

3. **Перейдите в Firestore Database:**
   - В левом меню: Build → Firestore Database

4. **Найдите коллекцию `users`:**
   - Нажмите на `users`

5. **Найдите пользователя:**
   - Найдите документ с email `detrayer15@gmail.com`
   - Нажмите на него

6. **Измените поля:**
   - Найдите поле `role` и измените на `developer`
   - Если поля `adminPoints` нет, добавьте его:
     - Нажмите "+ Add field"
     - Field: `adminPoints`
     - Type: `number`
     - Value: `9999`

7. **Сохраните** (кнопка "Update")

8. **Перезагрузите приложение**

---

## ✅ СПОСОБ 3: Ручное изменение в коде (для разработки)

Если Firebase не работает, можно временно изменить код:

### Файл: `src/store/authStore.ts`

Найдите функцию `login` или `setUser` и добавьте:

```typescript
setUser: (user) => {
    // Временно для разработки
    if (user.email === 'detrayer15@gmail.com') {
        user.role = 'developer';
        user.adminPoints = 9999;
    }
    set({ user });
}
```

---

## 🎯 Что даёт роль "developer"?

✅ Полный доступ ко всем страницам  
✅ Управление пользователями  
✅ **Изменение рангов** (только developer!)  
✅ Доступ к аналитике  
✅ Максимальный ранг: 🏆 Архитектор (9999 очков)  

---

## ❓ Если не работает:

### Проверьте:

1. **Приложение запущено?**
   ```bash
   npm run dev
   ```

2. **Пользователь зарегистрирован?**
   - Сначала зарегистрируйтесь через `/register`
   - Потом откройте `/dev-setup`

3. **Firebase подключён?**
   - Проверьте файл `.env`
   - Должны быть все ключи Firebase

---

## 🔥 РЕКОМЕНДУЮ:

**Способ 1** - самый простой!

Просто откройте: `http://localhost:5174/dev-setup`

Всё остальное произойдёт автоматически! 🚀
