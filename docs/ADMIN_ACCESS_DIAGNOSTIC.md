# 🔍 ДИАГНОСТИКА: Доступ админов к /admin/users

## ✅ КАК ДОЛЖНО РАБОТАТЬ:

### Доступ к странице `/admin/users`:
- ✅ **Admin** - ДОСТУП РАЗРЕШЁН (просмотр, изменение)
- ✅ **Developer** - ДОСТУП РАЗРЕШЁН (просмотр, изменение, удаление)
- ❌ **Student** - ДОСТУП ЗАПРЕЩЁН

### Проверка в коде:
**Файл:** `src/pages/AdminUsersPage.tsx`, строка 28
```typescript
if (currentUser?.role !== 'admin' && currentUser?.role !== 'developer') {
    // Показать "Доступ запрещён"
}
```

**Это правильно!** Админы должны проходить эту проверку.

---

## 🐛 ВОЗМОЖНЫЕ ПРИЧИНЫ ПРОБЛЕМЫ:

### 1. Роль не установлена
**Проверьте:**
- Зайдите в настройки (`/settings`)
- Посмотрите какая роль отображается
- Должно быть "Администратор" или "Разработчик"

### 2. Роль в Firebase не совпадает
**Проверьте в Firebase Console:**
- Откройте Firestore Database
- Найдите коллекцию `users`
- Найдите вашего пользователя
- Проверьте поле `role`
- Должно быть: `admin` или `developer`

### 3. Кеш браузера
**Попробуйте:**
- Выйти из аккаунта
- Очистить кеш (Ctrl+Shift+Delete)
- Войти заново

---

## 🔧 КАК ИСПРАВИТЬ:

### Если роль не установлена:

**Вариант 1: Через `/dev-setup`**
1. Откройте: `http://localhost:5173/dev-setup`
2. Роль автоматически станет `developer`
3. Перезагрузите страницу

**Вариант 2: Через Firebase Console**
1. Откройте Firebase Console
2. Firestore Database → `users`
3. Найдите пользователя
4. Измените `role` на `admin` или `developer`
5. Перезагрузите страницу

**Вариант 3: Через консоль браузера**
1. Откройте консоль (F12)
2. Вставьте:
```javascript
localStorage.clear()
location.reload()
```
3. Войдите заново

---

## ✅ ПРОВЕРКА:

### После установки роли:
1. Откройте `/settings`
2. Должно показать: "👑 Администратор" или "👑 Разработчик"
3. Откройте `/admin`
4. Должна быть кнопка "Пользователи"
5. Нажмите на неё
6. Страница должна открыться ✅

---

## 📊 ТЕКУЩИЙ СТАТУС:

**Код правильный:**
- ✅ Проверка доступа работает корректно
- ✅ Админы должны иметь доступ
- ✅ Кнопка "Удалить" только для developer (это правильно)

**Если не работает:**
- Проблема в роли пользователя
- Нужно проверить Firebase или установить роль заново

---

**Какая ошибка появляется когда админ пытается зайти на `/admin/users`?**
- "Доступ запрещён"?
- Пустая страница?
- Другое?
