# 🚀 Быстрый старт

## ✅ Что уже сделано:
- Backend создан (20+ файлов)
- Frontend интеграция готова
- Зависимости установлены

## 📝 Что нужно сделать:

### 1. Настроить MongoDB Atlas (3 минуты)

1. Зайти на https://www.mongodb.com/cloud/atlas/register
2. Создать FREE кластер (M0)
3. Создать пользователя БД:
   - Username: `eliteheat`
   - Password: придумайте и запомните
4. Разрешить доступ:
   - Network Access → Add IP → "Allow from Anywhere" (0.0.0.0/0)
5. Получить connection string:
   - Database → Connect → Drivers
   - Скопировать строку
   - Заменить `<password>` на ваш пароль

### 2. Настроить Backend

```bash
cd backend

# Скопировать .env.example в .env
copy .env.example .env

# Открыть .env в блокноте и вставить connection string:
# MONGODB_URI=mongodb+srv://eliteheat:ВАШ_ПАРОЛЬ@cluster0.xxxxx.mongodb.net/eliteheat

# Запустить сервер
npm run dev
```

### 3. Запустить Frontend

```bash
# В новом терминале, в корне проекта
npm run dev
```

### 4. Использовать

1. Открыть http://localhost:5173
2. Зарегистрироваться (не забудьте указать город!)
3. Войти в систему

### 5. Стать админом

**MongoDB Compass (рекомендуется):**
1. Скачать https://www.mongodb.com/try/download/compass
2. Подключиться (вставить connection string)
3. Открыть базу `eliteheat` → коллекцию `users`
4. Найти своего пользователя
5. Изменить `role: "student"` на `role: "admin"`
6. Сохранить

**Или через MongoDB Atlas:**
1. В Atlas → Database → Browse Collections
2. База `eliteheat` → коллекция `users`
3. Найти пользователя → Edit
4. Изменить `role` на `admin`

## 🎯 Доступные страницы:

- `/chat` - Чат (после создания второго пользователя)
- `/admin/users` - Админ-панель (только для админов)
- `/dashboard` - Главная
- `/settings` - Настройки

## ❓ Проблемы?

**Backend не запускается:**
- Проверьте connection string в `backend/.env`
- Убедитесь что MongoDB Atlas настроен правильно

**Frontend не подключается:**
- Убедитесь что backend запущен на порту 3000
- Проверьте консоль браузера (F12)

**Не могу войти:**
- Проверьте что backend запущен
- Проверьте консоль backend на ошибки
