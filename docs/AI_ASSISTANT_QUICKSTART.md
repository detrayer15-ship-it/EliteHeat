# 🚀 Быстрый Старт - AI Assistant

## Что сделано

✅ **Все фазы 1-6 завершены!**

- Firebase Firestore (security rules + indexes)
- Backend с "Учительским" форматом
- Frontend с чатами и форматированными ответами
- Real-time обновления

---

## Деплой

### Вариант 1: Через Firebase Console (Рекомендуется)

#### 1. Firestore Rules

1. Откройте [Firebase Console](https://console.firebase.google.com)
2. Выберите ваш проект
3. **Firestore Database** → **Rules**
4. Скопируйте содержимое файла `firestore.rules`
5. Вставьте и нажмите **Publish**

#### 2. Firestore Indexes

1. В Firebase Console: **Firestore Database** → **Indexes**
2. Нажмите **Add Index**
3. Создайте 2 индекса:

**Индекс 1:**
- Collection: `aiChats`
- Fields: `userId` (Ascending), `updatedAt` (Descending)

**Индекс 2:**
- Collection: `aiMessages`
- Fields: `chatId` (Ascending), `createdAt` (Ascending)

---

### Вариант 2: Через Firebase CLI

Если у вас установлен Firebase CLI:

```bash
# PowerShell (Windows) - с кавычками!
firebase deploy --only "firestore:rules,firestore:indexes"

# Bash/Terminal (Mac/Linux)
firebase deploy --only firestore:rules,firestore:indexes
```

Если не установлен:

```bash
npm install -g firebase-tools
firebase login
firebase deploy --only "firestore:rules,firestore:indexes"
```

---

### 3. Запустить Backend

```bash
cd backend
npm start
```

Должно появиться:
```
✅ Server running on port 3000
✅ Gemini AI initialized
```

### 4. Запустить Frontend

```bash
npm run dev
```

Откройте: `http://localhost:5173`

---

## Как использовать

1. Перейдите в **AI Assistant**
2. Нажмите **"+"** (новый чат)
3. Выберите режим:
   - 🎓 **Tutor** - обучение
   - 💻 **Developer** - код
   - 🐛 **Debug** - отладка
   - 💡 **Product** - идеи
4. Задайте вопрос

---

## Формат ответов

AI всегда отвечает структурированно:

```
Вопрос: [перефразировка вашего вопроса]
Тема: [предмет/раздел]
Ответ: [чёткий ответ]
Пример: [код или пример]
```

Если вопрос неясен:

```
Не хватает данных: [что именно]
Уточни: 1) ... 2) ...
```

---

## Тест

Попробуйте:
- "Привет" → AI поздоровается
- "Объясни циклы в Python" → Получите структурированный ответ
- "Объясни это" → AI попросит уточнить

---

## Готово! 🎉

Всё работает. Приятного использования!
