# ✅ API Integration Checklist

## 🎯 Проверьте перед использованием

### 1. Переменные окружения
- [ ] Файл `.env.local` существует
- [ ] `VITE_API_URL=http://localhost:3000` установлен
- [ ] `VITE_GEMINI_API_KEY` установлен
- [ ] Dev сервер перезапущен после изменения `.env.local`

### 2. Backend
- [ ] Backend установлен (`cd backend && npm install`)
- [ ] Backend запущен (`npm run dev:memory`)
- [ ] Backend работает на порту 3000
- [ ] Нет ошибок в консоли backend

### 3. Frontend
- [ ] Frontend запущен (`npm run dev`)
- [ ] Нет ошибок компиляции
- [ ] Сайт открывается на `localhost:5173`

### 4. API Модули
- [ ] `src/api/client.ts` создан
- [ ] `src/api/auth.ts` создан
- [ ] `src/api/admin.ts` создан
- [ ] `src/api/submissions.ts` создан
- [ ] `src/api/chats.ts` создан
- [ ] `src/api/courses.ts` создан
- [ ] `src/api/gemini.ts` обновлен
- [ ] `src/api/index.ts` создан
- [ ] `src/api/hooks.ts` создан

### 5. Тестирование
- [ ] Открыта страница `/developer/api-test`
- [ ] Тест регистрации работает
- [ ] Тест входа работает
- [ ] Тест Gemini AI работает
- [ ] Нет ошибок в консоли браузера (F12)

### 6. Документация
- [ ] `API_COMPLETE.md` прочитан
- [ ] `GEMINI_API_READY.md` прочитан
- [ ] `API_QUICKSTART.md` прочитан
- [ ] Понятно как использовать API

---

## 🚀 Быстрая проверка

Выполните эти команды для быстрой проверки:

### 1. Проверка переменных окружения
```bash
# Windows PowerShell
Get-Content .env.local
```

Должно показать:
```
VITE_API_URL=http://localhost:3000
VITE_GEMINI_API_KEY=AIza...
```

### 2. Проверка backend
```bash
# В папке backend
npm run dev:memory
```

Должно показать:
```
🚀 EliteHeat Backend Server (IN-MEMORY MODE)
📡 Server running on port 3000
```

### 3. Проверка frontend
```bash
npm run dev
```

Должно показать:
```
VITE ready in XXX ms
Local: http://localhost:5173/
```

### 4. Тест в браузере
Откройте консоль (F12) и выполните:
```javascript
// Проверка API URL
console.log(import.meta.env.VITE_API_URL)
// Должно показать: http://localhost:3000

// Проверка Gemini API
console.log(import.meta.env.VITE_GEMINI_API_KEY ? 'Gemini OK' : 'Gemini NOT SET')
// Должно показать: Gemini OK

// Проверка токена
console.log(localStorage.getItem('authToken') ? 'Token exists' : 'No token')
```

---

## ❗ Troubleshooting

### Проблема: "API ключ не найден"
**Решение:**
1. Проверьте `.env.local`
2. Перезапустите dev сервер
3. Очистите кеш браузера (Ctrl+Shift+R)

### Проблема: "Connection refused"
**Решение:**
1. Убедитесь что backend запущен
2. Проверьте порт 3000 (должен быть свободен)
3. Проверьте `VITE_API_URL` в `.env.local`

### Проблема: "401 Unauthorized"
**Решение:**
1. Сначала выполните `login()`
2. Проверьте что токен сохранился: `localStorage.getItem('authToken')`
3. Проверьте что токен добавляется к запросам (смотрите Network в DevTools)

### Проблема: "Module not found"
**Решение:**
1. Проверьте что все файлы созданы
2. Перезапустите dev сервер
3. Очистите node_modules и переустановите: `rm -rf node_modules && npm install`

---

## 📊 Финальная проверка

Все пункты должны быть ✅:

| Компонент | Статус |
|-----------|--------|
| .env.local настроен | ✅ |
| Backend запущен | ✅ |
| Frontend запущен | ✅ |
| API модули созданы | ✅ |
| Тестовая страница работает | ✅ |
| Нет ошибок в консоли | ✅ |
| Документация прочитана | ✅ |

---

## 🎉 Готово!

Если все пункты отмечены ✅ - можете начинать использовать API!

**Следующий шаг:** Откройте `API_QUICKSTART.md` и начните разработку!
