# 🏗️ API Архитектура EliteHeat

## 📊 Структура API

```
┌─────────────────────────────────────────────────────────────┐
│                    REACT КОМПОНЕНТЫ                          │
│  (Pages, Components, Features)                               │
└────────────────────┬────────────────────────────────────────┘
                     │
                     │ используют
                     ▼
┌─────────────────────────────────────────────────────────────┐
│                    REACT HOOKS                               │
│  useGeminiChat() | useCourses() | useSubmissions()          │
│  useChat() | useAPI()                                        │
└────────────────────┬────────────────────────────────────────┘
                     │
                     │ вызывают
                     ▼
┌─────────────────────────────────────────────────────────────┐
│                    API МОДУЛИ                                │
│  ┌──────────┬──────────┬──────────┬──────────┬──────────┐  │
│  │  auth.ts │ admin.ts │ chats.ts │courses.ts│gemini.ts │  │
│  │          │          │          │          │          │  │
│  │ register │getAllUsers│getMyChat│getAllCourses│sendText│  │
│  │  login   │getUserById│sendMsg  │getCourse │sendImage│  │
│  │  logout  │updateUser│markRead │enroll    │checkCode│  │
│  └──────────┴──────────┴──────────┴──────────┴──────────┘  │
│                                                               │
│  ┌──────────────────┐                                        │
│  │ submissions.ts   │                                        │
│  │                  │                                        │
│  │ submitAssignment │                                        │
│  │ getMySubmissions │                                        │
│  │ reviewSubmission │                                        │
│  └──────────────────┘                                        │
└────────────────────┬────────────────────────────────────────┘
                     │
                     │ используют
                     ▼
┌─────────────────────────────────────────────────────────────┐
│                    API CLIENT (Axios)                        │
│  • Базовый URL: http://localhost:3000                       │
│  • Автоматическое добавление токенов                         │
│  • Обработка ошибок (401, 500, etc)                         │
│  • Логирование запросов                                      │
└────────────────────┬────────────────────────────────────────┘
                     │
        ┌────────────┴────────────┐
        │                         │
        ▼                         ▼
┌──────────────┐         ┌──────────────────┐
│   BACKEND    │         │   GEMINI AI      │
│   API        │         │   API            │
│              │         │                  │
│ localhost:   │         │ Google Cloud     │
│   3000       │         │ Generative AI    │
└──────────────┘         └──────────────────┘
```

## 🔄 Поток данных

### 1. Аутентификация
```
Компонент → useAPI() → login() → API Client → Backend
                                       ↓
                                   Получить токен
                                       ↓
                                localStorage.setItem('authToken')
                                       ↓
                            Все последующие запросы
                            автоматически включают токен
```

### 2. Gemini AI запрос
```
Компонент → useGeminiChat() → sendTextMessage() → Gemini AI
                                       ↓
                                  Получить ответ
                                       ↓
                                  Вернуть в компонент
                                       ↓
                                  Показать пользователю
```

### 3. Работа с заданиями
```
Студент:
Компонент → useSubmissions() → submitAssignment() → Backend
                                       ↓
                                  Сохранить submission
                                       ↓
                                  Вернуть подтверждение

Учитель:
Компонент → useSubmissions() → getAllSubmissions() → Backend
                                       ↓
                                  Получить все submissions
                                       ↓
                                  reviewSubmission() → Backend
                                       ↓
                                  Обновить статус и оценку
```

## 🔐 Безопасность

```
┌─────────────────────────────────────────────────────────────┐
│                    ПЕРЕМЕННЫЕ ОКРУЖЕНИЯ                      │
│  .env.local (НЕ в Git!)                                     │
│  ├── VITE_API_URL=http://localhost:3000                     │
│  └── VITE_GEMINI_API_KEY=AIza...                            │
└────────────────────┬────────────────────────────────────────┘
                     │
                     │ используются в
                     ▼
┌─────────────────────────────────────────────────────────────┐
│                    API МОДУЛИ                                │
│  • Никаких хардкоженных ключей                              │
│  • Проверка наличия ключей                                  │
│  • Fallback режимы                                          │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    ТОКЕНЫ                                    │
│  localStorage.getItem('authToken')                           │
│  ├── Сохраняется при login()                                │
│  ├── Автоматически добавляется к запросам                   │
│  └── Удаляется при logout()                                 │
└─────────────────────────────────────────────────────────────┘
```

## 📡 Backend Endpoints

```
┌─────────────────────────────────────────────────────────────┐
│                    AUTH ENDPOINTS                            │
│  POST   /api/auth/register    - Регистрация                 │
│  POST   /api/auth/login       - Вход                        │
│  GET    /api/auth/me          - Текущий пользователь        │
│  POST   /api/auth/logout      - Выход                       │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    ADMIN ENDPOINTS                           │
│  GET    /api/admin/users      - Все пользователи            │
│  GET    /api/admin/users/:id  - Пользователь по ID          │
│  PUT    /api/admin/users/:id  - Обновить пользователя       │
│  DELETE /api/admin/users/:id  - Удалить пользователя        │
│  GET    /api/admin/stats      - Статистика                  │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    SUBMISSIONS ENDPOINTS                     │
│  POST   /api/submissions      - Отправить задание           │
│  GET    /api/submissions      - Все submissions (админ)     │
│  GET    /api/submissions/my   - Мои submissions (студент)   │
│  PUT    /api/submissions/:id/review - Проверить задание     │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    CHAT ENDPOINTS                            │
│  GET    /api/chats/my         - Мой чат                     │
│  GET    /api/chats/:id        - Чат по ID                   │
│  POST   /api/chats/:id/message - Отправить сообщение        │
│  PUT    /api/chats/:id/read   - Отметить прочитанным        │
└─────────────────────────────────────────────────────────────┘
```

## 🎨 Примеры использования

### В компоненте с хуками:
```typescript
import { useGeminiChat } from '@/api/hooks';

function AIChat() {
  const { sendMessage, loading, error } = useGeminiChat();

  const handleSend = async () => {
    const response = await sendMessage('Привет!');
    console.log(response);
  };

  return (
    <button onClick={handleSend} disabled={loading}>
      {loading ? 'Думаю...' : 'Отправить'}
    </button>
  );
}
```

### Прямой вызов API:
```typescript
import { login, getAllCourses } from '@/api';

// Вход
const response = await login({
  email: 'test@test.com',
  password: 'test123'
});

// Получить курсы
const courses = await getAllCourses();
```

## 🧪 Тестирование

```
┌─────────────────────────────────────────────────────────────┐
│                    API TEST PAGE                             │
│  URL: /developer/api-test                                   │
│                                                               │
│  ┌──────────┬──────────┬──────────┬──────────┬──────────┐  │
│  │   Test   │   Test   │   Get    │  Gemini  │   Get    │  │
│  │ Register │  Login   │  User    │    AI    │ Courses  │  │
│  └──────────┴──────────┴──────────┴──────────┴──────────┘  │
│                                                               │
│  Результаты:                                                 │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ ✅ Регистрация успешна!                             │    │
│  │ Пользователь: Test User                             │    │
│  │ Email: test@test.com                                │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

## 📊 Статус модулей

| Модуль | Функций | Статус | Тестирование |
|--------|---------|--------|--------------|
| auth.ts | 4 | ✅ | ✅ |
| admin.ts | 5 | ✅ | ✅ |
| submissions.ts | 5 | ✅ | ✅ |
| chats.ts | 5 | ✅ | ✅ |
| courses.ts | 8 | ✅ | ✅ |
| gemini.ts | 5 | ✅ | ✅ |
| hooks.ts | 5 | ✅ | ✅ |
| client.ts | 1 | ✅ | ✅ |

**Всего:** 38 функций, 8 модулей, 100% готовность

---

## 🚀 Готово к использованию!

Вся API инфраструктура создана, протестирована и готова к использованию в продакшене!
