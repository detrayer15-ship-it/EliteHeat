# 🚀 БЫСТРАЯ НАСТРОЙКА BACKEND

## Проблема
Backend запущен, но не подключён к MongoDB, поэтому регистрация не работает.

## Решение (5 минут)

### Шаг 1: Создать MongoDB Atlas аккаунт

1. Открыть https://www.mongodb.com/cloud/atlas/register
2. Зарегистрироваться (можно через Google)
3. Создать FREE кластер (M0) - выбрать ближайший регион
4. Создать пользователя:
   - Username: `eliteheat`
   - Password: `eliteheat123` (или свой)
5. Network Access → Add IP → "Allow from Anywhere" (0.0.0.0/0)
6. Database → Connect → Drivers → Скопировать connection string

### Шаг 2: Создать .env файл

В папке `backend` создать файл `.env` (без расширения!) с таким содержимым:

```env
NODE_ENV=development
PORT=3000

MONGODB_URI=mongodb+srv://eliteheat:eliteheat123@cluster0.XXXXX.mongodb.net/eliteheat?retryWrites=true&w=majority

JWT_SECRET=eliteheat-secret-key-123
JWT_EXPIRES_IN=7d

FRONTEND_URL=http://localhost:5173

RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
```

**ВАЖНО:** Замените `XXXXX` в MONGODB_URI на ваш реальный адрес кластера из MongoDB Atlas!

### Шаг 3: Перезапустить backend

1. Остановить backend (Ctrl+C в терминале где запущен backend)
2. Запустить снова:
```bash
cd backend
npm run dev
```

### Шаг 4: Проверить

Должно появиться:
```
✅ MongoDB Connected: cluster0.xxxxx.mongodb.net
🚀 EliteHeat Backend Server
📡 Server running on port 3000
```

Теперь регистрация заработает! ✅

---

## Альтернатива: Использовать мой тестовый MongoDB

Если не хотите создавать свой аккаунт, можете временно использовать тестовую БД.

Создайте `.env` с:
```env
MONGODB_URI=mongodb+srv://test:test123@cluster0.example.mongodb.net/eliteheat
```

(Это пример, реальный connection string я не могу создать без вашего аккаунта)

---

## Нужна помощь?

Если не получается - дайте знать на каком шаге застряли!
